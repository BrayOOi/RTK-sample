// Add hooks that are used frequently together here
// Can always import directly from the respective dedicated files if the hook is to be consumed independently
import useAppDispatch from './useAppDispatch';
import useAppSelector from './useAppSelector';

export { useAppDispatch, useAppSelector };
